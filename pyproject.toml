[project]
name = "regulens"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "feedparser>=6.0.12",
    "httpx>=0.28.1",
    "jupyter",
    "pandas",
    "requests",
]
